
// Verificar si el botón tiene el atributo "disabled"
const botonElement = document.getElementById('miBoton');
const hasDisabledAttribute = botonElement.hasAttribute('disabled');

console.log('¿El botón tiene el atributo "disabled"?: ' + hasDisabledAttribute);
