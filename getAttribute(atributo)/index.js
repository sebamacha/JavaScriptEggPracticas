// Obtener el valor del atributo "src" de la imagen
const imagenElement = document.getElementById('miImagen');
const srcValue = imagenElement.getAttribute('src');

console.log('El valor del atributo "src" es: ' + srcValue);
