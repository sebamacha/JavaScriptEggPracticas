// Verificar si el párrafo tiene atributos
const parrafoElement = document.getElementById('miParrafo');
const hasAttributes = parrafoElement.hasAttributes();

console.log('¿El párrafo tiene atributos?: ' + hasAttributes);
